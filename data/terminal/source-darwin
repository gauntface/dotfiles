nano_editor=$(which nano)
export EDITOR="$nano_editor"
export VISUAL="$EDITOR"

coreutils_dir="/opt/homebrew/opt/coreutils/libexec/gnubin"
if [[ -d $coreutils_dir ]]; then
    export PATH="$coreutils_dir:$PATH"
fi
